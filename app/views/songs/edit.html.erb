<h1>Update Song</h1>

<%= form_for @song do |f| %>
    <%= f.label "Name:" %>
    <br>
    <%= f.text_field :name, value: @song.name %> 
    <br>
    <%= f.label "Artist ID:" %>
    <br>
    <%= f.text_field :artist_id, value: @song.artist.id %>
    <br>
    <%= f.label "Genre ID:" %>
    <br>
    <%= f.text_field :genre_id, value: @song.genre.id %>
    <br>
    <%= f.submit %>
<% end %>