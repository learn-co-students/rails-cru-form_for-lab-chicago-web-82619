<h1>Update Artist</h1>

<%= form_for @artist do |f| %>
    <%= f.label "Name:" %>
    <br>
    <%= f.text_field :name, value: @artist.name %> 
    <br>
    <%= f.label "Bio:" %>
    <br>
    <%= f.text_area :bio, value: @artist.bio %>
    <br>
    <%= f.submit %>
<% end %>